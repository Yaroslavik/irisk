{% extends '@App/layout.html.twig' %}

{% block content %}
    <div class="clearfix">
        <div class="container">
            <h1>{{ page.title }}</h1>
            {{ page.content|raw }}
        </div>
    </div>
    {% include '@App/slideshow.html.twig' %}
{% endblock %}